# Cheerio Parse

Simple script that is parsing text from matching elements on given url.
Give it a url like https://example.com and CSS selector.

## Installation

```bash
git clone https://github.com/cxnexion/cheery-parse/
cd cheery-parse
npm install
npm run start
```

## Usage

```bash
npm start <url> <CSSSelector>
```